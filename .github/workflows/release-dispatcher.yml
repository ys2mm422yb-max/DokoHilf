name: DokoHilf Legacy Build 27 Validation (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Explain retirement
        run: |
          echo "Die alte PR-53-/Cloud-TTS-Prüfung ist dauerhaft stillgelegt."
          echo "Aktuelle v28-Releases werden ausschließlich durch Deploy DokoHilf geprüft."
