name: Publish DokoHilf to gh-pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: dokohilf-gh-pages-publish
  cancel-in-progress: true

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4

      - name: Build static Pages directory
        run: |
          set -euo pipefail
          mkdir -p _site/assets
          cp index.html editor.html manifest.webmanifest icon.svg service-worker.js version.json _site/
          cp \
            assets/styles.css \
            assets/premium-ui-v25.css \
            assets/premium-ui-v26.css \
            assets/dark-ui-v27.css \
            assets/experience-v26.js \
            assets/experience-v27.js \
            assets/update-manager.js \
            assets/mobile-audio-fix.js \
            assets/voice-diagnostics.js \
            assets/routing-fix.js \
            assets/conversation-intelligence.js \
            assets/clarification-ui.js \
            assets/guide-progress.js \
            assets/voice-focus-mode.js \
            assets/app.js \
            assets/editor.css \
            assets/editor.js \
            _site/assets/
          touch _site/.nojekyll

          test -s _site/index.html
          test -s _site/version.json
          test -s _site/assets/premium-ui-v25.css
          test -s _site/assets/premium-ui-v26.css
          test -s _site/assets/dark-ui-v27.css
          test -s _site/assets/experience-v26.js
          test -s _site/assets/experience-v27.js
          grep -q 'dokohilf-build" content="20260806-27' _site/index.html
          grep -q 'KI · v27' _site/index.html
          grep -q 'dark-ui-v27.css?v=20260806-27' _site/index.html
          grep -q 'experience-v27.js?v=20260806-27' _site/index.html
          grep -q '"buildId": "20260806-27"' _site/version.json
          grep -q "BUILD_ID = '20260806-27'" _site/service-worker.js
          grep -q '__DOKOHILF_DARK_UI_V27__' _site/assets/experience-v27.js
          grep -q '__DOKOHILF_VOICE_RECOVERY_V27__' _site/assets/experience-v27.js
          grep -q -- '--v27-bg:#020b13' _site/assets/dark-ui-v27.css

      - name: Publish exact site to gh-pages
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          cd _site
          git init --initial-branch=gh-pages
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add --all
          git commit -m "Publish DokoHilf ${GITHUB_SHA}"
          git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git push --force origin HEAD:gh-pages
